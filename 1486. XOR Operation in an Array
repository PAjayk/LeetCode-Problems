class Solution(object):
    def xorOperation(self, n, start):
        nums=[]
        for i in range(0,n):
            k = start + 2*i
            nums.append(k)
        s=nums[0]
        for j in range(1,n):
            s=s^nums[j]
        return s
            
            
        
