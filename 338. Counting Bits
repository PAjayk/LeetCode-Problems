class Solution:
    def countBits(self, n: int) -> List[int]:
        l=[]
        for i in range(n+1):
            k=bin(i).replace("0b","")
            z=k.count("1")
            l.append(z)
        return l
        
